export const enum ENUM_MENU_TYPE {
	MAIN = 'main',
	REMOVE_WHITESPACE = 'remove_whitespace',
}

export const enum ENUM_BUTTON_TYPE {
	CLOSE = 'close',
	SUBMIT = 'submit',
	RESET = 'reset',
	COPY = 'copy',
}
