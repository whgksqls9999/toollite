import styled from '@emotion/styled';

export const RadioWrapper = styled.label`
	cursor: pointer;
	width: min-content;
	white-space: nowrap;
	border: 1px solid black;
	border-radius: 5px;
	padding: 0.3rem;
`;
