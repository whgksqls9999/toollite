import { memo } from 'react';
import { ENUM_MENU_TYPE } from '../enum/common';

export const RemoveWhiteSpacePage = memo(() => {
	const menu = ENUM_MENU_TYPE.REMOVE_WHITESPACE;

	return <div>RemoveWhiteSpacePage</div>;
});
